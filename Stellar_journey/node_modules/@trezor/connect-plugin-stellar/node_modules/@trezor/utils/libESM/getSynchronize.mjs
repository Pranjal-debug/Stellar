import { getMutex } from "./getMutex.mjs";
export const getSynchronize = mutex => {
  const lock = mutex ?? getMutex();
  return (action, lockId) => lock(lockId).then(unlock => Promise.resolve().then(action).finally(unlock));
};