import { type CallMethodAnyResponse, type CallMethodPayload } from '@trezor/connect-common/libESM/events';
import type { ConnectImpl, ConnectImplSettings } from '@trezor/connect-common/libESM/impl/dynamic';
import { type Log } from '@trezor/connect-common/libESM/utils/debug';
export declare class CoreInSuiteWeb implements ConnectImpl {
    private _popupManager?;
    protected logger: Log;
    constructor();
    dispose(): Promise<undefined>;
    cancel(reason?: string): void;
    init({ env, manifest, version, debug }: ConnectImplSettings): Promise<void>;
    private getSuiteUrl;
    call(params: CallMethodPayload): Promise<CallMethodAnyResponse>;
}
