import { type CoreEventMessage } from '@trezor/connect-common/libESM/events';
import { type AbstractMessageChannel } from '@trezor/connect-common/libESM/messageChannel/abstract';
import { Popup } from './abstract';
export declare class WebPopup extends Popup {
    private popupWindow;
    private iframe;
    private channelId;
    protected createChannel(): AbstractMessageChannel<CoreEventMessage>;
    protected open(): Promise<void>;
    protected focusPopup(): void;
    protected closePopup(): void;
    protected isOpen(): Promise<boolean>;
    protected onReset(): void;
}
