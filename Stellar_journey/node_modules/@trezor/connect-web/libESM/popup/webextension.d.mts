import { type CoreEventMessage } from '@trezor/connect-common/libESM/events';
import { type AbstractMessageChannel } from '@trezor/connect-common/libESM/messageChannel/abstract';
import { Popup } from './abstract';
export declare class WebExtensionPopup extends Popup {
    private popupWindow?;
    private popupWindowPromise;
    private extensionTabId;
    protected buildPopupUrl(src: string): string;
    private checkIfTabExists;
    private logChromeError;
    protected createChannel(origin: string): AbstractMessageChannel<CoreEventMessage>;
    protected open(): Promise<void>;
    private openPopupInNewWindow;
    private openPopupInNewTab;
    private onPopupTabResolved;
    protected focusPopup(): void;
    protected closePopup(): void;
    protected isOpen(): Promise<boolean>;
    protected onReset(focus?: boolean): void;
}
