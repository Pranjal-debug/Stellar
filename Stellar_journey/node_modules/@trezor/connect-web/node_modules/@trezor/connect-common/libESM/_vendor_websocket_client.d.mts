import * as _trezor_utils from '@trezor/utils';
import { TypedEmitter } from '@trezor/utils';
import WebSocket from 'ws';

type WebsocketOptions = {
    url: string;
    timeout?: number;
    agent?: WebSocket.ClientOptions['agent'];
    headers?: WebSocket.ClientOptions['headers'];
};
type Options = WebsocketOptions & {
    pingTimeout?: number;
    connectionTimeout?: number;
    keepAlive?: boolean;
    concurrency?: number;
    onSending?: (message: Record<string, any>) => void;
};
type WebsocketClientEvents = {
    error: string;
    disconnected: undefined;
};
type WebsocketSendParams = {
    timeout?: number;
    onIdCreated?: (id: number) => void;
};
type WebsocketRequest = Record<string, any>;
type WebsocketResponse = WebSocket.Data;
declare class WebsocketError extends Error {
}
declare class WebsocketClient<Events extends Record<string, any>> extends TypedEmitter<Events & WebsocketClientEvents> {
    readonly options: Options;
    protected readonly messages: _trezor_utils.DeferredManager<any, number>;
    private readonly emitter;
    private ws?;
    private pingTimeout?;
    private connectPromise?;
    protected createWebsocket?(): WebSocket;
    protected ping(): Promise<any>;
    constructor(options: Options);
    protected initWebsocket({ url, timeout, headers, agent }: WebsocketOptions): WebSocket;
    private setPingTimeout;
    protected onPing(): Promise<any>;
    onMessageTimeout(_promiseId: number): void;
    private onError;
    sendMessage(message: WebsocketRequest, { timeout, onIdCreated }?: WebsocketSendParams): Promise<any>;
    protected sendRawMessage(message: WebSocket.Data): void;
    protected onMessage(message: WebsocketResponse, messageValidation?: (data: Record<string, any>) => Record<string, any> | void): void;
    connect(): Promise<void>;
    private init;
    disconnect(): Promise<void>;
    isConnected(): boolean;
    private onClose;
    dispose(): void;
}

export { WebsocketClient, WebsocketError };
export type { WebsocketRequest, WebsocketResponse, WebsocketSendParams };
