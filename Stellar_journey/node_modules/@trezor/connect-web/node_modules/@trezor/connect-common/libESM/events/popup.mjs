import { UI_EVENT } from "./ui-request.mjs";
export const POPUP = {
  CORE_LOADED: 'popup-core-loaded',
  INIT: 'popup-init',
  HANDSHAKE: 'popup-handshake',
  CLOSED: 'popup-closed',
  CLOSE_WINDOW: 'window.close'
};
export const createPopupMessage = (type, payload) => ({
  event: UI_EVENT,
  type,
  payload
});