import { AbstractMessageChannel, type AbstractMessageChannelConstructorParams } from './abstract.mjs';
export declare class ServiceWorkerWindowExtConnectableChannel<IncomingMessages extends {
    type: string;
}> extends AbstractMessageChannel<IncomingMessages> {
    private currentId?;
    private allowedOrigin?;
    private messageListener?;
    private sendChain;
    private static SEND_SETTLE_MS;
    constructor({ channel, logger, currentId, allowedOrigin, }: Pick<AbstractMessageChannelConstructorParams, 'channel' | 'logger'> & {
        currentId?: () => Promise<number | undefined> | undefined;
        allowedOrigin?: string;
    });
    private doSend;
    clearPendingSends(): void;
    connect(): void;
    disconnect(): void;
}
