import { AbstractMessageChannel, type AbstractMessageChannelConstructorParams, type Message } from './abstract.mjs';
export declare class WindowWindowChannel<IncomingMessages extends {
    type: string;
    channel?: {
        peer: string;
        here: string;
    };
    payload?: any;
}> extends AbstractMessageChannel<IncomingMessages> {
    _windowHere: Window;
    _listener: typeof WindowWindowChannel.prototype.listener;
    _origin: string;
    constructor({ windowHere, windowPeer, channel, logger, origin, }: Pick<AbstractMessageChannelConstructorParams, 'channel' | 'logger'> & {
        windowHere: Window;
        windowPeer: () => Window | undefined;
        origin: string;
    });
    listener(event: MessageEvent<Message<IncomingMessages>>): void;
    connect(): void;
    disconnect(): void;
}
