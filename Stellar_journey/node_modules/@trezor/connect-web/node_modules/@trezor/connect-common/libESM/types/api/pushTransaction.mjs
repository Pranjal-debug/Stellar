import { Type } from "@trezor/schema-utils";
export const PushTransaction = Type.Object({
  tx: Type.Union([Type.String(), Type.Object({
    hex: Type.String(),
    disableAlternativeRPC: Type.Optional(Type.Boolean())
  })]),
  coin: Type.String(),
  identity: Type.Optional(Type.String())
});