export type GetObjectWithKey<U, K extends PropertyKey> = U extends object ? K extends keyof U ? U : never : never;
export type GetObjectWithoutKey<U, K extends PropertyKey> = U extends object ? K extends keyof U ? never : U : never;
export type ObjectsOnly<T> = T extends Record<string, unknown> ? T : never;
export type KeysOfUnion<T> = T extends T ? keyof T : never;
export type RequireAtLeastOne<T, K extends keyof T = keyof T> = {
    [P in K]-?: Pick<T, P> & Partial<Omit<T, P>>;
}[K] & Omit<T, K>;
export type NarrowObjectWithKey<T, K extends PropertyKey> = T extends any ? K extends keyof T ? T : never : never;
export type NullablePropsRecursive<T extends Record<string, any>> = {
    [K in keyof T]: T[K] extends (...args: any[]) => any ? T[K] | null : T[K] extends Record<string, any> ? NullablePropsRecursive<T[K]> | null : T[K] | null;
};
