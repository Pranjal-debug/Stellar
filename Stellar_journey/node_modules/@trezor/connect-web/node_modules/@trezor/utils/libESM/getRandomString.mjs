import { getRandomInt } from "./getRandomInt.mjs";
const DEFAULT_ALPHABET = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
const MAX_ALPHABET_LENGTH = 0xffffffff + 1;
export const getRandomString = (length, alphabet = DEFAULT_ALPHABET) => {
  if (!Number.isSafeInteger(length) || length < 1) {
    throw new RangeError(`The "length" argument must be a positive safe integer. Received ${length}`);
  }
  if (alphabet.length < 2) {
    throw new RangeError(`The alphabet must contain at least 2 characters. Received ${alphabet.length}`);
  }
  if (alphabet.length > MAX_ALPHABET_LENGTH) {
    throw new RangeError(`The alphabet must contain at most ${MAX_ALPHABET_LENGTH} characters. Received ${alphabet.length}`);
  }
  if (new Set(alphabet).size !== alphabet.length) {
    throw new RangeError('The alphabet must contain unique characters.');
  }
  let result = '';
  for (let i = 0; i < length; i++) {
    result += alphabet.charAt(getRandomInt(0, alphabet.length));
  }
  return result;
};